= Скриншоты интерфейса программного модуля <appendix:b>

#set par(first-line-indent: 0cm)

#align(center)[
  #text(14pt, weight: "bold")[Скриншоты интерфейса программного модуля]
]

#v(0.5cm)

На рисунках Б.1–Б.6 представлены скриншоты основных страниц пользовательского интерфейса программного модуля оценки производственных рисков.

#figure(
  rect(width: 100%, stroke: 0.5pt, inset: 10pt, align(center)[
    #text(style: "italic", size: 12pt)[Скриншот страницы «Дашборд» — сводные показатели и графики]
    #v(4pt)
    #text(size: 10pt)[Дашборд отображает: общее количество инцидентов, отказов оборудования и нарушений ТБ,\
    графики распределения по подразделениям, тяжести, типам оборудования и месячный тренд.]
  ]),
  caption: [Страница «Дашборд»],
) <fig:screenshot_dashboard>

#figure(
  rect(width: 100%, stroke: 0.5pt, inset: 10pt, align(center)[
    #text(style: "italic", size: 12pt)[Скриншот страницы «Загрузка данных» — drag-and-drop загрузка файлов]
    #v(4pt)
    #text(size: 10pt)[Страница позволяет загрузить CSV или Excel файлы с данными. Система автоматически\
    определяет тип данных по структуре столбцов и сохраняет в базу данных.]
  ]),
  caption: [Страница «Загрузка данных»],
) <fig:screenshot_upload>

#figure(
  rect(width: 100%, stroke: 0.5pt, inset: 10pt, align(center)[
    #text(style: "italic", size: 12pt)[Скриншот страницы «Данные» — табличное представление с фильтрами]
    #v(4pt)
    #text(size: 10pt)[Страница содержит три вкладки: инциденты, отказы оборудования, нарушения ТБ.\
    Данные отображаются в таблице с цветовой индикацией тяжести.]
  ]),
  caption: [Страница «Данные»],
) <fig:screenshot_data>

#figure(
  rect(width: 100%, stroke: 0.5pt, inset: 10pt, align(center)[
    #text(style: "italic", size: 12pt)[Скриншот страницы «FMEA-анализ» — результаты автоматического расчёта]
    #v(4pt)
    #text(size: 10pt)[Страница позволяет выбрать подразделение и тип оборудования. Результаты включают\
    таблицу с RPN, столбчатую диаграмму и рекомендации по устранению рисков.]
  ]),
  caption: [Страница «FMEA-анализ»],
) <fig:screenshot_fmea>

#figure(
  rect(width: 100%, stroke: 0.5pt, inset: 10pt, align(center)[
    #text(style: "italic", size: 12pt)[Скриншот страницы «Статистика» — вкладка «Тренд-анализ»]
    #v(4pt)
    #text(size: 10pt)[График отображает фактические данные, линию тренда, прогноз с 95%-ным\
    доверительным интервалом. Доступна группировка по месяцам, кварталам, годам.]
  ]),
  caption: [Страница «Статистика», вкладка «Тренд-анализ»],
) <fig:screenshot_trend>

#figure(
  rect(width: 100%, stroke: 0.5pt, inset: 10pt, align(center)[
    #text(style: "italic", size: 12pt)[Скриншот страницы «Экспорт» — формирование PDF-отчёта]
    #v(4pt)
    #text(size: 10pt)[Страница позволяет скачать комплексный PDF-отчёт по ГОСТ с результатами всех видов\
    анализа, а также экспортировать исходные данные в формате Excel.]
  ]),
  caption: [Страница «Экспорт»],
) <fig:screenshot_export>
